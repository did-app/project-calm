# Erlang version
erlang_version=22.3

# Elixir version
elixir_version=1.10.2

# A command to run right after compiling the app
# "User does not have CONNECT privilege" heroku DB comes with a create DB
hook_post_compile="mix setup"
